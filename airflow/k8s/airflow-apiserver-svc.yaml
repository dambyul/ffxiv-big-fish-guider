apiVersion: v1
kind: Service
metadata:
  namespace: fish
  labels:
    service: airflow-apiserver
  name: airflow-apiserver
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000
  selector:
    service: airflow-apiserver